# Task_3. Для навигации в файловой системе нужно отличать директории от остальных типов файлов
# Определите типы этих файлов:

# /dev/null ; character special file

# /dev/zero ; character special file

# /dev/sda ; i have not had this file at my computer, however, it is the disk SCSI (small computer system interface)

# /dev/tty ; character special file

# /root ; directory

# /etc/passwd ; regular file

# /proc/mounts ; symbolic link

# /bin/ls ; regular file

# /bin/zcat ; regular file
